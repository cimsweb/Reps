services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: reps
      POSTGRES_USER: reps
      POSTGRES_PASSWORD: reps
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
